# Galery-React-App
search app for images using react with type script .
in this app we use typescript in react instead of javascript, also we use some libraries like redux/toolkit .
we use PixaBay api to fetch data
you can search images and see their informations
